<div className="profileButtonOptions">
                      <button className="followButton" onClick={handleClick}>
                        {alreadyRequested && "Already Requested"}
                        {!alreadyRequested && !gotRequest && "Send Request"}
                        {gotRequest && "Accept Request"}
                        {/* {followed ? "Unfollow" : "Follow"}
                      {followed ? <Remove /> : <Add />} */}
                      </button>
                      {gotRequest && (
                        <button className="followButton" onClick={handleClick}>
                          {alreadyRequested && "Already Requested"}
                          {!alreadyRequested && !gotRequest && "Send Request"}
                          {gotRequest && "Reject Request"}
                          {/* {followed ? "Unfollow" : "Follow"}
                      {followed ? <Remove /> : <Add />} */}
                        </button>
                      )}
                    </div>